import React from "react";
import MapComponent from "../features/map/MapComponent"; // sesuaikan path

const MapPage = () => {
  return (
    <main className="w-full h-screen">
      <MapComponent />
    </main>
  );
};

export default MapPage;
